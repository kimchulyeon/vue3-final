import { describe, it, expect } from "vitest";

describe("Basic Math", () => {
  it("add two numbers", () => {
    expect(1 + 1).toBe(2);
  });

  it("substracts two numbers", () => {
    expect(5 - 1).toBe(4);
  });
});
